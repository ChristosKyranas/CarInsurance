package CarInsuranceUtils.Factories;

import CarInsuranceUtils.Validations.InputOptionValidator;
import CarInsuranceUtils.Validations.InputPlateValidator;

public class ValidatorFactory {

    /*public static InputValidator getInstance(int sourceType) {

        if(sourceType == 1) {
            InputValidator validator = new InputOptionValidator();
            return validator;
        }else if (sourceType == 2) {
            InputValidator validator = new InputPlateValidator();
            return validator;
        }
    }*/
}
